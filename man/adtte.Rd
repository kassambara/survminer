% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adtte.R
\docType{data}
\name{adtte}
\alias{adtte}
\title{Example CDISC ADTTE Time-to-Event Dataset}
\format{
A data frame with 520 rows (260 subjects x 2 parameters) and 9 columns.
 \describe{
 \item{\code{STUDYID}}{study identifier}
 \item{\code{USUBJID}}{unique subject identifier}
 \item{\code{TRT01P}}{planned treatment: \code{"Placebo"} or
   \code{"Active Treatment"}}
 \item{\code{PARAMCD}}{parameter code: \code{"OS"} (overall survival) or
   \code{"PFS"} (progression-free survival)}
 \item{\code{PARAM}}{parameter description}
 \item{\code{AVAL}}{analysis value: the time to event or censoring, in months}
 \item{\code{AVALU}}{analysis value unit (\code{"MONTHS"})}
 \item{\code{CNSR}}{censoring flag: \code{0} = event, \code{1} = administratively
   censored (alive/ongoing at the data cutoff), \code{2} = lost to follow-up}
 \item{\code{EVNTDESC}}{event or censoring description}
 }
}
\usage{
data("adtte")
}
\description{
A small, simulated CDISC ADaM time-to-event analysis dataset
 (\code{ADTTE}) for demonstrating \code{\link{surv_adtte}()}. It follows the
 ADaM structure and the \code{CNSR} censoring convention (\strong{0 = event,
 \eqn{\ge 1} = censored}), with two analysis parameters and a clear treatment
 effect. The data are entirely synthetic (no real patients).
}
\details{
Simulated with a strong treatment benefit (Active Treatment vs Placebo:
 median overall survival roughly 34 vs 12 months), so the Kaplan-Meier curves
 separate clearly (log-rank \eqn{p < 0.0001}) for both parameters. Because
 \code{CNSR} uses the CDISC coding (0 = event), it must be converted before use
 in \code{\link[survival]{Surv}()} -- which is what \code{\link{surv_adtte}()}
 does.
}
\examples{
data("adtte")
head(adtte)

os <- surv_adtte(adtte, paramcd = "OS")
fit <- surv_fit(survival::Surv(AVAL, event) ~ TRT01P, data = os)
ggsurvplot(fit, data = os, pval = TRUE, risk.table = TRUE)

}
\seealso{
\code{\link{surv_adtte}}
}
